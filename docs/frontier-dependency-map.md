# Frontier run — dependency map (no survivor)

- rounds: 2; near-misses: 0

## Deaths
- r1 [scorecard] Is the RT minimal surface the same variational object as the loss-landscape geodesic, and does either side predict a scaling law the other has not reported?: mock: killed by literature_gap axis — canned prior work exists
- r2 [scorecard] What 'spacetime' do codes that do NOT come from known lattices imply?: mock: killed by literature_gap axis — canned prior work exists

Math is not physics; every claim above requires expert review.
